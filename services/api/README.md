# AELIA API

Reserved for the production API service.

Planned stack: Node.js + TypeScript + PostgreSQL + Redis/queue where needed + object storage + model adapter + agent orchestration.

Initial endpoints:
GET /health
POST /v1/chat
POST /v1/projects
POST /v1/conversations
GET /v1/capabilities
POST /v1/tasks
